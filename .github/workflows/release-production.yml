name: "Stencil ESLint Plugin Production Release"

on:
  workflow_call:
    inputs:
      bump:
        description: "Semver bump to apply (patch, minor, major)."
        required: true
        type: choice
        default: patch
        options:
          - patch
          - minor
          - major

permissions:
  contents: read
  id-token: write

jobs:
  build_eslint_plugin:
    name: 🏗️ Build ESLint Plugin
    uses: ./.github/workflows/build.yml

  publish_production:
    name: 🚀 Publish Production Build
    needs: build_eslint_plugin
    uses: ./.github/workflows/publish-npm.yml
    with:
      tag: latest
      version: ${{ inputs.bump }}
