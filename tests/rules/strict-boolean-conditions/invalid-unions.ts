const maybe: string | undefined = Math.random() > 0.5 ? 'x' : undefined;
if (maybe) {}
const boolOrUndefined: boolean | undefined = Math.random() > 0.5 ? true : undefined;
while (boolOrUndefined) {}

